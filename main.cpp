#include <iostream>
#include "Network.h"

using namespace std;


int main() {

   /* Network net(topology);

    net.feedForward(inputVals);
    net.backProp(targetVals);
    net.getResults(resultVals);*/

}